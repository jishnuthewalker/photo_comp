// vitest@4 removed defineWorkspace — workspace support is via --config pointing
// at each package's vitest.config.ts.  This file is kept for documentation only.
// Root `test` script runs: vitest run --config packages/shared/vitest.config.ts
